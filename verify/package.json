{
  "name": "surface-browser-parity",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Runs the browser's own decode path (site/js/detect.js) under onnxruntime-node, and the published page itself under real Chrome, and compares both detection-for-detection with the Python ultralytics model. Dev-only: nothing here is served to the page.",
  "scripts": {
    "reference": "cd ../.. && .venv/bin/python site/verify/reference_ultralytics.py --dump-pixels /tmp/refpx",
    "parity": "node parity.mjs --pixels /tmp/refpx --out parity_js.json",
    "parity:jpegjs": "node parity.mjs --pixels /tmp/refpx --force-jpeg --out parity_js_jpegjs.json",
    "compare": "node compare.mjs --json parity_report.json",
    "serve": "cd .. && python3 -m http.server 8777 --bind 127.0.0.1",
    "parity:browser": "node browser_parity.mjs --url http://127.0.0.1:8777/ --json parity_browser.json",
    "all": "npm run reference && npm run parity && npm run parity:jpegjs && npm run compare"
  },
  "devDependencies": {
    "jpeg-js": "0.4.4",
    "onnxruntime-node": "1.23.2",
    "puppeteer-core": "^24.43.1"
  }
}
